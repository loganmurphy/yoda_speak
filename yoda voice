function say (text, voice) {
  var speech = new SpeechSynthesisUtterance(text);
  speech.voice = voices[8];
  speech.rate = .575;
  speech.volume = 0.8;
  speech.pitch = .5;
  speechSynthesis.speak(speech);
}

say('Yoda. I am. Old and green. I am. Jedi master I am.')
